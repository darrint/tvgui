context.objects = [
  { factory = adapter
    args = {
      factory.name = api.alsa.pcm.sink
      node.name = "alsa-hdmi"
      node.description = "HDMI"
      media.class = "Audio/Sink"
      api.alsa.path = "hdmi:CARD=vc4hdmi,DEV=0"
      api.alsa.disable-mmap = true
      audio.format = "S16LE"
      audio.rate = 48000
      audio.channels = 2
      node.pause-on-idle = false
      session.suspend-timeout-seconds = 0
    }
  }
]
